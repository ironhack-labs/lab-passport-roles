<div>
  <h1>LISTA DE USUARIOS</h1>
  {{#each users}}
    <h2>{{username}}</h2> 
    <a href="/auth/userlist/delete/{{_id}}">Eliminar</a>
    <b>Roles:</b>
    {{#each this.role}}
      <i> {{this}}</i>
    {{/each}} 
  {{/each}}
  <div>
    <br>
    <br>
    <a href="/">HOME</a>
  </div>
</div>