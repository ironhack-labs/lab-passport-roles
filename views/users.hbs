<div class="container"></div>

  <div class="list-group">
    <h2>
      Users:
    </h2>

    <div class="row  align-items-center">
      {{#each allUsers}}
      <div class="col-md-10">
        <a href="/users/details/{{_id}}" class="list-group-item list-group-item-action">{{name}}</a>
      </div>

      <div class="col-md-2">
        <form action="/users/delete?id={{_id}}" method="POST">
          <input name="id" type="hidden" class="form-control" id="field1">
          {{#if ../isBoss}}
          <button class"delete" type="submit" class="btn btn-primary">Delete</button>
          {{/if}}
          <a href="/users/{{_id}}/edit" class="btn btn-primary">Edit</a>
        </form>
      </div>
      {{/each}}
    </div>
  </div>
  {{#if isBoss}}
  <a id="create" href="/users/add" class="btn btn-primary">Create a new employee</a>
  {{/if}}
