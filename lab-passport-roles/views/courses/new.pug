extends ../layout

block content

  h2 Add Course

  .form-container
    form(action="/courses" method='POST')

      .form-element
        label(for="name") Name
        input(type="name" id="name" name="name")

      if error 
        p #{error}
      button(type="submit") Add new Course